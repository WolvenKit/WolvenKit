<Window
    x:Class="WolvenKit.Views.Dialogs.Windows.ShowNewPlayerHeadDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
    Title="Select body gender"
    MinWidth="{DynamicResource WolvenKitDialogWidthSmall}"
    MinHeight="{DynamicResource WolvenKitDialogHeightSmall}"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner">
    <syncfusion:WizardControl
        CancelButtonCancelsWindow="True"
        FinishButtonClosesWindow="True">
        <syncfusion:WizardPage
            BackVisibility="Collapsed"
            CancelVisibility="Visible"
            FinishVisibility="Visible"
            HelpVisibility="Collapsed"
            NextVisibility="Collapsed"
            KeyDown="ShowNewPlayerHeadDialogView_OnKeyDown"
            PageType="Exterior">

            <Grid Height="Auto">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="5" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="5" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{DynamicResource WolvenKitDialogLabelColumnWidthMedium}" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.MergedDictionaries>
                            <hc:ThemeResources />
                            <hc:Theme />
                        </ResourceDictionary.MergedDictionaries>
                        <Style
                            x:Key="LabelStyle"
                            TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>
                    </ResourceDictionary>

                </Grid.Resources>

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    Style="{StaticResource LabelStyle}"
                    Text="Female" />

                <RadioButton
                    Grid.Row="0"
                    Grid.Column="1"
                    Checked="BodyGenderFemale" />

                <TextBlock
                    Grid.Row="2"
                    Grid.Column="0"
                    Style="{StaticResource LabelStyle}"
                    Text="Male" />

                <RadioButton
                    Grid.Row="2"
                    Grid.Column="1"
                    Checked="BodyGenderMale" />
            </Grid>

        </syncfusion:WizardPage>
    </syncfusion:WizardControl>
</Window>
