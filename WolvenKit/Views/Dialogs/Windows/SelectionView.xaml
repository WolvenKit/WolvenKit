<Window
    x:Class="WolvenKit.Views.Dialogs.Windows.SelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
    Title="{Binding Title}"
    MinWidth="{DynamicResource WolvenKitDialogWidthSmall}"
    MinHeight="{DynamicResource WolvenKitDialogHeightSmall}"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner"
    DataContext="{Binding RelativeSource={RelativeSource Self},
                          Path=ViewModel}">
    <Grid
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        hc:ThemeManager.RequestedAccentColor="{DynamicResource MahApps.Brushes.Accent3}">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <hc:ThemeResources />
                    <hc:Theme />
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>

        <syncfusion:WizardControl
            CancelButtonCancelsWindow="True"
            FinishButtonClosesWindow="True">
            <syncfusion:WizardPage
                BackVisibility="Collapsed"
                CancelVisibility="Visible"
                FinishVisibility="Visible"
                HelpVisibility="Collapsed"
                NextVisibility="Collapsed"
                PreviewKeyDown="WizardPage_PreviewKeyDown"
                PageType="Exterior">
                <StackPanel
                    Margin="{DynamicResource WolvenKitMarginTiny}"
                    Orientation="Vertical">

                    <TextBlock
                        Margin="{DynamicResource WolvenKitMarginSmallBottom}"
                        Style="{StaticResource WolvenkitTextStyle}"
                        Visibility="{Binding Path=ShowText,
                                             Converter={StaticResource BooleanToVisibilityConverter}}"
                        TextWrapping="Wrap"
                        Text="{Binding Text}" />

                    <ComboBox
                        x:Name="Dropdown"
                        Visibility="{Binding Path=AllowMultiSelect,
                                             Converter={StaticResource InvertBooleanVisibilityConverter}}"
                        ItemsSource="{Binding Options}"
                        SelectedValue="{Binding SelectedOption}" />

                    <ListBox
                        x:Name="ListDropdown"
                        Height="{DynamicResource WolvenKitDataGridRowHeight}"
                        Visibility="{Binding Path=AllowMultiSelect,
                                             Converter={StaticResource BooleanVisibilityConverter}}"
                        ItemsSource="{Binding Options}"
                        SelectionMode="Multiple">

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="1.0" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </StackPanel>
            </syncfusion:WizardPage>
        </syncfusion:WizardControl>
    </Grid>
</Window>