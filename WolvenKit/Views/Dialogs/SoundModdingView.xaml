<reactiveUi:ReactiveUserControl
    x:TypeArguments="dialogs:SoundModdingViewModel"
    x:Class="WolvenKit.Views.Dialogs.SoundModdingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:WolvenKit.Views.Dialogs"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:reactiveUi="http://reactiveui.net"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:dialogs="clr-namespace:WolvenKit.ViewModels.Dialogs;assembly=WolvenKit.App"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
    mc:Ignorable="d" 
    d:DesignHeight="600" d:DesignWidth="800"
    Width="800"
    Height="600"
    Background="{StaticResource ContentBackgroundAlt3}"
    d:DataContext="{d:DesignInstance Type=dialogs:SoundModdingViewModel}"
    >

    <Grid Margin="20">

       

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Sound Modding Tool" FontSize="42" Grid.Row="0"/>
        <Separator Grid.Row="1"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <syncfusion:SfDataGrid x:Name="DataGridEvents" Margin="5"
                                       ItemsSource="{Binding EventNames}"
                                       AllowFiltering="True"
                                       ColumnSizer="Star"
                                       SelectionMode="Extended"
                                       AllowResizingColumns="True"
                                   
                           AutoGenerateColumns="False" 
                          >
                    <syncfusion:SfDataGrid.Columns>
                        <syncfusion:GridTextColumn  HeaderText="Name" MappingName="Name" IsReadOnly="true"/>
                        <!--<syncfusion:GridTemplateColumn MappingName="Tags">
                            <syncfusion:GridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <syncfusion:ComboBoxAdv
                                        ItemsSource="{Binding Tags}"
                                        AllowMultiSelect="true"
                                        IsEditable="false"
                                        EnableToken="true">
                                    </syncfusion:ComboBoxAdv>
                                </DataTemplate>
                            </syncfusion:GridTemplateColumn.CellTemplate>
                        </syncfusion:GridTemplateColumn>-->
                    </syncfusion:SfDataGrid.Columns>
                </syncfusion:SfDataGrid>

                <StackPanel Grid.Row="1">
                    <Button Content="Add Events" Click="Button_Click"/>
                </StackPanel>
            </Grid>

            <syncfusion:SfGridSplitter Grid.Column="1" HorizontalAlignment="Stretch" 
                                   ResizeBehavior="PreviousAndNext"
                                   Width="auto"/>

            <ListView Grid.Column="2" x:Name="PluginList" HorizontalContentAlignment="Stretch" Margin="5"
                 ItemsSource="{Binding Events}"
                 SelectedItem="{Binding SelectedEvent}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>


                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" FontSize="14" Margin="2"/>
                                <syncfusion:ComboBoxAdv x:Name="comboBoxFile" 
                                                        ItemsSource="{Binding Files}"
                                                        SelectedValue="{Binding File}"/>
                                <syncfusion:ComboBoxAdv x:Name="comboBoxType" Height="30" Width="200" SelectedItem="{Binding Type}" ItemsSource="{Binding Types}"/>

                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <hc:NumericUpDown DecimalPlaces="2" Value="{Binding Pitch}"/>
                                <hc:NumericUpDown DecimalPlaces="2" Value="{Binding Gain}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
           
            </ListView>
        </Grid>
        
        <Grid Grid.Row="3"  Height="28" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Save" Command="{Binding SaveCommand}" Margin="10,0,0,0" Padding="12,0" HorizontalAlignment="Right"/>
            <Button Grid.Column="2" Content="Cancel" Command="{Binding CancelCommand}" Margin="10,0,0,0" Padding="12,0" HorizontalAlignment="Right"/>

        </Grid>
    </Grid>
</reactiveUi:ReactiveUserControl>
