<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WolvenKit.Views.Templates">
    <Style TargetType="{x:Type local:TruncatingTextBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TruncatingTextBox}">
                    <Grid>
                        <TextBox x:Name="DisplayTextBox" Text="{Binding DisplayText, RelativeSource={RelativeSource TemplatedParent}}"
                                 Visibility="{Binding HasNoFocus, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <TextBox x:Name="EditTextBox" Text="{Binding EditText, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                 Visibility="{Binding HasFocus, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>