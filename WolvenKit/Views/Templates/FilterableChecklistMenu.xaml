<UserControl
    x:Class="WolvenKit.Views.Templates.FilterableChecklistMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:WolvenKit.Functionality.Helpers"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:converters="clr-namespace:WolvenKit.Converters"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
    FocusManager.FocusedElement="{Binding ElementName=originalFilterTextBox}"
    DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid>
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <hc:ThemeResources />
                    <hc:Theme />
                </ResourceDictionary.MergedDictionaries>
                <Style
                    BasedOn="{StaticResource WolvenkitDialogLabelStyleMedium}"
                    TargetType="{x:Type Label}" />
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Background" Value="{StaticResource BackgroundColor_Control}" />
                </Style>
                <converters:IntToVisibilityConverterInverted x:Key="IntToVisibilityConverterInverted" />
            </ResourceDictionary>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{StaticResource WolvenKitDialogLabelColumnWidthMedium}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Label -->
        <Label
            Grid.Row="0"
            Grid.Column="0"
            Width="{StaticResource WolvenKitLabelWidthMedium}"
            HorizontalAlignment="Left"
            Background="#252525"
            BorderThickness="0"
            Content="{Binding Label}" />

        <!-- Filter TextBox -->
        <TextBox
            x:Name="originalFilterTextBox"
            Grid.Row="0"
            Grid.Column="1"
            Margin="0"
            Foreground="{StaticResource ForegroundColor_Grey_Dark}"
            helpers:TextBoxBehavior.TripleClickSelectAll="True"
            KeyboardNavigation.TabIndex="1"
            Text="{Binding FilterText,
                           UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            x:Name="originalPlaceholder"
            Grid.Row="0"
            Grid.Column="1"
            Style="{StaticResource WolvenkitPlaceholderStyle}"
            Visibility="{Binding ElementName=originalFilterTextBox,
                                 Path=Text.Length,
                                 Converter={StaticResource IntToVisibilityConverterInverted}}"
            Text="Filter" />

        <!-- ListBox for Options -->
        <ListBox
            x:Name="originalDropdown"
            Grid.Row="2"
            Grid.Column="1"
            MinHeight="{StaticResource WolvenKitRedTreeRowMinHeight}"
            MaxHeight="{StaticResource WolvenKitDialogHeightMedium}"
            SelectionMode="Multiple"
            SelectionChanged="ListBox_OnSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox
                        Margin="2"
                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem},
                                            Path=IsSelected,
                                            Mode=TwoWay}">
                        <CheckBox.Content>
                            <TextBlock
                                Text="{Binding}"
                                TextDecorations="{x:Null}" />
                        </CheckBox.Content>
                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Focusable" Value="False" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>