<UserControl
 x:Class="WolvenKit.Views.Shell.DockingAdapter"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 xmlns:local="clr-namespace:WolvenKit.Views.Shell"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
 xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
 xmlns:docvm="clr-namespace:WolvenKit.ViewModels.Documents;assembly=WolvenKit.App"
 xmlns:converters1="clr-namespace:WolvenKit.Converters"
 d:DesignHeight="300"
 d:DesignWidth="300"
 mc:Ignorable="d"             
 >

    <!--UserControl.Resources>
        <Style x:Key="TabItemStyle1" TargetType="{x:Type TabItem}" >
            <Setter Property="Header" Value="{Binding Path=(syncfusion:DockingManager.Header)}" />
            <Setter Property="Syncfusion:DockingManager.ListenTabItemEvents" Value="True" />
            <Setter Property="MinWidth" Value="21" />
            <Setter Property="MinHeight" Value="21" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Tag" Value="IsInternalTabItem" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid Name="Transform">
                            <syncfusion:ContextMenuBorder Name="Border" Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"BorderThickness="1">
                                <Border.ContextMenu>
                                    <syncfusion:CustomContextMenu Name="PART_ContextMenu"  Focusable="false"  />
                                </Border.ContextMenu>
                                <Border.LayoutTransform>
                                    <RotateTransform Angle="0" />
                                </Border.LayoutTransform>
                                <DockPanel LastChildFill="True">
                                    <Image DockPanel.Dock="Left" Margin="1" Height="13" Style="{DynamicResource FileIcons}"/>
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center"
                  ContentSource="Header" Margin="2,2,2,2" RecognizesAccessKey="True"
                  ContentTemplate="{Binding Path=(TabItem.Content).(Syncfusion:DockingManager.HeaderTemplate),
                  RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type TabItem}}}"/>
                                </DockPanel>
                            </syncfusion:ContextMenuBorder>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
            <syncfusion:DockingManager.Resources>
                <DataTemplate x:Key="RedDocumentDockingHeader" DataType="{x:Type docvm:RedDocumentViewModel}">
                    <DockPanel ToolTip="{Binding RelativePath}">
                        <Image x:Name="DockIcon" Width="13" Margin="0,0,4,0" Source="{svgc:SvgImage /Resources/Media/Images/Icons/Files/mesh.svg}" Style="{DynamicResource FileIcons}" />
                        <TextBlock
                        x:Name="TemplateTextBlock"
                        Grid.Column="1"
                        Margin="0,0,0,1"
                        Foreground="White"
                        Text="{TemplateBinding}"
                        ToolTip="{Binding RelativePath}"/>
                    </DockPanel>
                </DataTemplate>
                <DataTemplate x:Key="BasicDockingHeader">
                    <DockPanel>
                        <TextBlock
                        x:Name="TemplateTextBlock"
                        Grid.Column="1"
                        Margin="0,0,0,1"
                        Foreground="White"
                        Text="{TemplateBinding}"/>
                    </DockPanel>
                </DataTemplate>
                <converters1:DockingManagerHeaderTemplateSelector x:Key="dockingManagerHeaderTemplateSelector"/>
            </syncfusion:DockingManager.Resources>
    </UserControl.Resources-->
    <Grid>

        <syncfusion:DockingManager x:Name="PART_DockingManager"
                                   ActiveWindowChanged="PART_DockingManager_ActiveWindowChanged_1"
                                   Background="Transparent"
                                   DocumentCloseButtonType="Extended"
                                   DraggingType="BorderDragging"
                                   PersistState="True"
                                   UseDocumentContainer="True"
                                   Loaded="PART_DockingManager_Loaded"
                                   CloseButtonClick="PART_DockingManagerOnCloseButtonClick"
                                   DockStateChanging="PART_DockingManagerOnDockStateChanging"
                                   DocumentTabItemStyle="{StaticResource RedDocumentTabItemExtStyle}">

            <!--syncfusion:DockingManager.DocumentTabItemStyle>
                <Style TargetType="{x:Type syncfusion:TabItemExt}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type syncfusion:TabItemExt}">
                                <DockPanel ToolTip="{Binding RelativePath}" x:Name="PART_DockPanel">
                                    <Image x:Name="DockIcon" Width="13" Margin="0,0,4,0" Source="{svgc:SvgImage /Resources/Media/Images/Icons/Files/mesh.svg}" Style="{DynamicResource FileIcons}" DataContext="" />
                                    <ToggleButton
                                        Name="PART_CloseButton" Width="15" Height="14" Margin="1,0,10,0" VerticalAlignment="Top"                                 
                                        DockPanel.Dock="Right" 
                                        Focusable="False" 
                                        Visibility="Collapsed"
                                        Command="syncfusion:TabControlCommands.CloseTabItem" />
                                    <Image
                                        x:Name="PART_Image"
					                    Source="{TemplateBinding syncfusion:TabControlExt.Image}"
					                    Height="{TemplateBinding syncfusion:TabControlExt.ImageHeight}"
					                    Width="{TemplateBinding syncfusion:TabControlExt.ImageWidth}"
					                    Stretch="Fill" Margin="{TemplateBinding IconMargin}"
					                    SnapsToDevicePixels="True" VerticalAlignment="Center"
                                        DockPanel.Dock="{Binding Path=(syncfusion:TabControlExt.ImageAlignment), Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                    <ContentPresenter
                                        ContentSource="Header" x:Name="Content" Margin="{TemplateBinding HeaderMargin}" TextElement.Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type syncfusion:TabItemExt}},FallbackValue=Transparent}" 
                                        RecognizesAccessKey="False" Content="{TemplateBinding HeaderedContentControl.Header}" 
                                        ContentTemplate="{TemplateBinding HeaderedContentControl.HeaderTemplate}"                                           
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                        SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                    <ContentPresenter
                                        ContentSource="Header" x:Name="PART_EditHeader" Visibility="Collapsed"
                                        Margin="{TemplateBinding HeaderMargin}" TextElement.Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type syncfusion:TabItemExt}},FallbackValue=Transparent}" 
                                        RecognizesAccessKey="True" Content="{TemplateBinding HeaderedContentControl.Header}"               
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                        SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                </DockPanel>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </syncfusion:DockingManager.DocumentTabItemStyle-->
            <!--ContentControl-->
            <!--/ContentControl-->
            <!--syncfusion:DockingManager.HeaderTemplate>
                <DataTemplate>
                    <DockPanel>
                        <TextBlock
                                x:Name="TemplateTextBlock"
                                Grid.Column="1"
                                Margin="0,0,0,1"
                                Foreground="White"
                                Text="{Binding}" />
                    </DockPanel>
                </DataTemplate>
            </syncfusion:DockingManager.HeaderTemplate-->
            <!--syncfusion:DockingManager.DocumentTabItemStyle>
                <Style TargetType="Image">
                    <Setter Property="Source" Value="{svgc:SvgImage /Resources/Media/Images/Icons/Files/mesh.svg}" />
                </Style>
            </syncfusion:DockingManager.DocumentTabItemStyle-->
            <!--syncfusion:DockingManager.Icon>
                <ImageBrush ImageSource="{svgc:SvgImage /Resources/Media/Images/Icons/Files/mesh.svg}" Stretch="Uniform" />
            </syncfusion:DockingManager.Icon-->

            <!--DockedElementTabbedHost.HostedElement.Content == RedDocumentViewModel-->
            <!--Rectangle Fill="White" Margin="0,0,4,0" Width="13" Height="13">
                                <Rectangle.OpacityMask>
                                    <ImageBrush Stretch="Uniform" ImageSource="{svgc:SvgImage /Resources/Media/Images/Icons/Files/mesh.svg}" />
                                </Rectangle.OpacityMask>
                            </Rectangle-->
            <!--syncfusion:DockingManager.HeaderTemplate>
                    <DataTemplate DataType="{x:Type docvm:RedDocumentViewModel}">
                    <DockPanel ToolTip="{Binding RelativePath}">
                        <Image Visibility="Collapsed" x:Name="DockIcon" Width="13" Margin="0,0,4,0" Source="{svgc:SvgImage /Resources/Media/Images/Icons/Files/mesh.svg}" Style="{DynamicResource FileIcons}" />
                        <TextBlock
                                x:Name="TemplateTextBlock"
                                Grid.Column="1"
                                Margin="0,0,0,1"
                                Foreground="White"
                                Text="{Binding}"
                                ToolTip="{Binding RelativePath}"/>
                    </DockPanel>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=Content, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type syncfusion:DockedElementTabbedHost}}}" Value="RedDocumentViewModel">
                            <Setter Property="Visibility" Value="Visible" TargetName="DockIcon"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </syncfusion:DockingManager.HeaderTemplate-->
        </syncfusion:DockingManager>
    </Grid>
</UserControl>
